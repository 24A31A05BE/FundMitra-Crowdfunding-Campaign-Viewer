fundmitra/
│── client/          # Frontend code
│── server/          # Backend code
│── models/          # Database schemas
│── routes/          # API routes
│── controllers/     # Logic handling
│── config/          # DB & environment configs
│── public/          # Static files
│── README.md
git clone https://github.com/your-username/fundmitra.git
cd fundmitra
cd server
npm install
cd client
npm install
PORT=5000
MONGO_URI=your_mongodb_connection
JWT_SECRET=your_secret_key
npm start
npm start
